# policyholder_system

使用 Express (node:v21.3.0) + TypeSrcipt 開發

```sh
# mysql build and run
1. docker-compose up -d

# change directory
2. cd policyholder_system

# run server
2. npm run start

# migrate seed data
4. npm run migrate
```